"use client";

export function MemberSettingsStudio({ sections }: { sections: readonly { key: string; title: string; description: string; items: readonly { label: string; value: string }[] }[] }) {
  return <div className="text-zinc-400">Settings Studio — rebuilding...</div>;
}
